'use client'
import {useEffect,useState} from 'react'
import {DollarSign,FileSignature,FileText,FolderKanban,HardHat,Home,Plus,Receipt,Sparkles,Camera,Users,CalendarDays,WalletCards,ClipboardList,NotebookPen,Settings,Menu,X,ChevronDown,Coins,LogIn,LogOut,Trash2,PlusCircle,Clock3,AlertCircle} from 'lucide-react'
import ContractWorkspace from './components/ContractWorkspace'
import JobTracker from './components/JobTracker'
import BlueprintLibrary from './components/BlueprintLibrary'
import ProjectGallery from './components/ProjectGallery'
import ReceiptOrganizer from './components/ReceiptOrganizer'
type Project={id:number;customer:string;name:string;status:string;date:string;amount:number};type AppData={projects:any[];customers:any[];invoices:any[];notes:any[];estimates:any[];docs:any[];receipts:any[];schedule:any[];employees:any[];payroll:any[];files:any[];timeEntries:any[];settings:any};const emptyData:AppData={projects:[],customers:[],invoices:[],notes:[],estimates:[],docs:[],receipts:[],schedule:[],employees:[],payroll:[],files:[],timeEntries:[],settings:{businessName:'',phone:'',email:'',address:'',estimateTerms:'Estimates are subject to field verification and final owner approval.'}};const groups=[{label:'Work',items:[['Dashboard',Home],['Projects',FolderKanban],['Schedule',CalendarDays],['Customers',Users]]},{label:'Create',items:[['AI Estimates',Sparkles],['Contracts',FileSignature],['Change Orders',ClipboardList],['Blueprint Library',FileText]]},{label:'Money',items:[['Invoices',Receipt],['Receipts',WalletCards],['Payroll',DollarSign],['Billing & Tokens',Coins]]},{label:'Business',items:[['Before & After',Camera],['Employees',HardHat],['Documents',FileText],['Notes',NotebookPen],['Settings',Settings]]}] as any[];const money=(n:number)=>'$'+Number(n||0).toLocaleString(undefined,{maximumFractionDigits:2});function norm(d:any):AppData{return{...emptyData,...d,projects:d?.projects||[],customers:d?.customers||[],invoices:d?.invoices||[],notes:d?.notes||[],estimates:d?.estimates||[],docs:d?.docs||[],receipts:d?.receipts||[],schedule:d?.schedule||[],employees:d?.employees||[],payroll:d?.payroll||[],files:d?.files||[],timeEntries:d?.timeEntries||[],settings:{...emptyData.settings,...(d?.settings||{})}}}async function ai(mode:string,input:string){const r=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode,input})});const b=await r.json().catch(()=>({}));if(!r.ok)throw Error('AI draft could not be generated.');return String(b.text||'')}
export default function Page(){const[section,setSection]=useState('Dashboard'),[data,setData]=useState<AppData>(emptyData),[loaded,setLoaded]=useState(false),[company,setCompany]=useState('BuildFlow Company'),[activeJob,setActiveJob]=useState<Project|null>(null),[toolsOpen,setToolsOpen]=useState(false),[createOpen,setCreateOpen]=useState(false);useEffect(()=>{fetch('/api/workspace',{cache:'no-store'}).then(async r=>{if(r.status===401){location.href='/login';return}const w=await r.json();setCompany(w.companyName||'BuildFlow Company');setData(norm(w.data||{}));setLoaded(true)})},[]);useEffect(()=>{if(!loaded)return;const t=setTimeout(()=>fetch('/api/workspace',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),450);return()=>clearTimeout(t)},[data,loaded]);if(!loaded)return <div className="loading"><HardHat/><b>Loading workspace…</b></div>;const go=(s:string)=>{if(s==='Billing & Tokens'){location.href='/subscribe';return}setSection(s);setActiveJob(null);setToolsOpen(false);setCreateOpen(false)};const createItems=[['Project','Projects'],['AI Estimate','AI Estimates'],['Invoice','Invoices'],['Receipt','Receipts'],['Contract','Contracts'],['Change Order','Change Orders'],['Customer','Customers']] as const;return <div className="compactShell"><header className="topbar"><div className="topBrand"><span className="topMark"><HardHat size={19}/></span><div><b>BuildFlow HQ</b><small>{data.settings.businessName||company}</small></div></div><nav className="quickNav"><button className={section==='Dashboard'?'active':''} onClick={()=>go('Dashboard')}><Home/>Home</button><button className={section==='Projects'?'active':''} onClick={()=>go('Projects')}><FolderKanban/>Jobs</button><button className={section==='AI Estimates'?'active':''} onClick={()=>go('AI Estimates')}><Sparkles/>Estimate</button><button className={section==='Receipts'?'active':''} onClick={()=>go('Receipts')}><WalletCards/>Receipts</button><button className="toolsBtn" onClick={()=>setToolsOpen(!toolsOpen)}><Menu/>More<ChevronDown size={14}/></button></nav>{toolsOpen&&<div className="toolMenu"><div className="toolMenuHead"><b>BuildFlow Tools</b><button onClick={()=>setToolsOpen(false)}><X/></button></div>{groups.map(g=><div className="toolGroup" key={g.label}><small>{g.label}</small><div>{g.items.map(([n,I]:any)=><button key={n} onClick={()=>go(n)} className={section===n?'active':''}><I size={17}/><span>{n}</span></button>)}</div></div>)}<div className="toolGroup"><small>Account</small><div><button onClick={()=>location.href='/login'}><LogIn size={17}/>Sign in</button><button onClick={()=>location.href='/logout'}><LogOut size={17}/>Sign out</button></div></div></div>}</header>{createOpen&&<div className="createMenu"><div className="createMenuHead"><b>Create New</b><button onClick={()=>setCreateOpen(false)}><X size={18}/></button></div>{createItems.map(([label,target])=><button key={label} onClick={()=>go(target)}><Plus size={17}/>{label}</button>)}</div>}<main className="compactMain"><div className="pageTitle"><h1>{section}</h1><p>{data.settings.businessName||company}</p></div>{activeJob?<JobTracker project={data.projects.find((p:any)=>p.id===activeJob.id)||activeJob} data={data} setData={setData} onClose={()=>setActiveJob(null)}/>:section==='Contracts'?<ContractWorkspace data={data} setData={setData} ai={ai}/>:section==='Blueprint Library'?<BlueprintLibrary data={data} setData={setData} ai={ai}/>:section==='Before & After'?<ProjectGallery data={data} setData={setData}/>:section==='Receipts'?<ReceiptOrganizer data={data} setData={setData}/>:section==='Projects'?<Projects data={data} setData={setData} startJob={setActiveJob}/>:section==='Schedule'?<Schedule data={data} setData={setData}/>:section==='Dashboard'?<Dashboard data={data} go={go}/>:<Simple section={section} data={data}/>}<footer>BuildFlow HQ · Contractor business workspace</footer></main><nav className="mobileNav"><button className={section==='Dashboard'?'active':''} onClick={()=>go('Dashboard')}><Home/><span>Home</span></button><button className={section==='Projects'?'active':''} onClick={()=>go('Projects')}><FolderKanban/><span>Jobs</span></button><button className="mobileCreate" onClick={()=>setCreateOpen(!createOpen)}><PlusCircle/><span>New</span></button><button className={section==='Schedule'?'active':''} onClick={()=>go('Schedule')}><CalendarDays/><span>Schedule</span></button><button onClick={()=>setToolsOpen(!toolsOpen)}><Menu/><span>More</span></button></nav></div>}
function Dashboard({data,go}:{data:AppData,go:(s:string)=>void}){const today=new Date().toISOString().slice(0,10),todayJobs=data.schedule.filter((x:any)=>x.date===today),unpaid=data.invoices.filter((x:any)=>String(x.status||'').toLowerCase()!=='paid'),unpaidTotal=unpaid.reduce((a:number,x:any)=>a+Number(x.amount||x.balance||0),0),recent=[...data.estimates].slice(0,4);return <><section className="hero"><div><span>CONTRACTOR HQ</span><h2>Your business, organized for today.</h2><p>See what needs attention and jump straight into the next job, estimate, or invoice.</p></div><button onClick={()=>go('AI Estimates')}><Plus size={17}/> New Estimate</button></section><section className="stats"><button className="statButton" onClick={()=>go('Projects')}><Stat label="Active Jobs" value={String(data.projects.filter((p:any)=>p.status==='In Progress').length)} sub="currently active"/></button><button className="statButton" onClick={()=>go('Schedule')}><Stat label="Today's Jobs" value={String(todayJobs.length)} sub="scheduled today"/></button><button className="statButton" onClick={()=>go('Invoices')}><Stat label="Unpaid" value={money(unpaidTotal)} sub={unpaid.length+" open invoices"}/></button></section><section className="dashboardGrid"><div className="dashPanel"><div className="dashPanelHead"><div><small>TODAY</small><h3>Today's Jobs</h3></div><button onClick={()=>go('Schedule')}>View schedule</button></div>{todayJobs.length?todayJobs.slice(0,4).map((x:any)=><div className="dashRow" key={x.id}><Clock3/><div><b>{x.project}</b><span>{x.start}–{x.end} · {x.worker}</span></div></div>):<EmptyAction icon={CalendarDays} text="Nothing scheduled for today." action="Add to schedule" onClick={()=>go('Schedule')}/>}</div><div className="dashPanel"><div className="dashPanelHead"><div><small>MONEY</small><h3>Unpaid Invoices</h3></div><button onClick={()=>go('Invoices')}>View invoices</button></div>{unpaid.length?unpaid.slice(0,4).map((x:any)=><div className="dashRow" key={x.id}><AlertCircle/><div><b>{x.customer||x.project||'Invoice'}</b><span>{x.status||'Open'}</span></div><strong>{money(x.amount||x.balance)}</strong></div>):<EmptyAction icon={Receipt} text="No unpaid invoices." action="Create invoice" onClick={()=>go('Invoices')}/>}</div><div className="dashPanel fullPanel"><div className="dashPanelHead"><div><small>RECENT</small><h3>Recent Estimates</h3></div><button onClick={()=>go('AI Estimates')}>New estimate</button></div>{recent.length?recent.map((x:any)=><div className="dashRow" key={x.id}><Sparkles/><div><b>{x.project||x.customer||'Estimate'}</b><span>{x.status||'Draft'}</span></div>{x.amount!==undefined&&<strong>{money(x.amount)}</strong>}</div>):<EmptyAction icon={Sparkles} text="No estimates yet." action="Create your first AI estimate" onClick={()=>go('AI Estimates')}/>}</div></section></>}
function EmptyAction({icon:Icon,text,action,onClick}:{icon:any,text:string,action:string,onClick:()=>void}){return <div className="emptyAction"><Icon/><p>{text}</p><button className="primary" onClick={onClick}>{action}</button></div>}
function Schedule({data,setData}:{data:AppData,setData:(d:any)=>void}){const[date,setDate]=useState(''),[start,setStart]=useState('08:00'),[end,setEnd]=useState('16:00'),[project,setProject]=useState(''),[worker,setWorker]=useState(''),[notes,setNotes]=useState('');function add(){if(!date||!project||!worker.trim())return;setData({...data,schedule:[{id:Date.now(),date,start,end,project,worker:worker.trim(),notes:notes.trim()},...data.schedule]});setNotes('')}const sorted=[...data.schedule].sort((a:any,b:any)=>String(a.date+a.start).localeCompare(String(b.date+b.start)));return <div className="workspace"><Head title="Schedule" text="Assign workers to jobs and set their work times."/><div className="builderGrid"><label>Date<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Start time<input type="time" value={start} onChange={e=>setStart(e.target.value)}/></label><label>End time<input type="time" value={end} onChange={e=>setEnd(e.target.value)}/></label><label>Job<select value={project} onChange={e=>setProject(e.target.value)}><option value="">Select job</option>{data.projects.map((p:any)=><option key={p.id} value={p.name}>{p.name} · {p.customer}</option>)}</select></label><label>Worker<input value={worker} onChange={e=>setWorker(e.target.value)} placeholder="Worker name"/></label><label>Notes<input value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Address, task, instructions…"/></label><button className="primary" onClick={add}><CalendarDays size={16}/> Add to Schedule</button></div>{sorted.length?sorted.map((s:any)=><div className="wideRow" key={s.id}><CalendarDays size={18}/><div className="grow"><b>{s.project}</b><span>{s.worker} · {s.date} · {s.start}–{s.end}{s.notes?' · '+s.notes:''}</span></div><button className="iconButton" title="Delete schedule item" onClick={()=>setData({...data,schedule:data.schedule.filter((x:any)=>x.id!==s.id)})}><Trash2 size={15}/></button></div>):<p>No work scheduled yet.</p>}</div>}
function Projects({data,setData,startJob}:{data:AppData,setData:(d:any)=>void,startJob:(p:Project)=>void}){const[c,setC]=useState(''),[n,setN]=useState(''),[a,setA]=useState('');function add(){if(!c||!n)return;setData({...data,projects:[{id:Date.now(),customer:c,name:n,status:'Estimate',date:'Not scheduled',amount:Number(a||0)},...data.projects]});setC('');setN('');setA('')}return <div className="workspace"><Head title="Projects" text="Start a job to track expenses and labor."/><div className="builderGrid"><Field label="Customer" value={c} set={setC}/><Field label="Project" value={n} set={setN}/><Field label="Estimate" value={a} set={setA}/><button className="primary" onClick={add}><Plus/>New Project</button></div>{data.projects.map((p:any)=><div className="wideRow" key={p.id}><div className="grow"><b>{p.name}</b><span>{p.customer}</span></div><strong>{money(p.amount)}</strong><span className="badge">{p.status}</span><button className="primary" onClick={()=>startJob(p)}>{p.status==='In Progress'?'Open Job':'Start Job'}</button></div>)}</div>}
function Simple({section,data}:{section:string,data:AppData}){const rows=section==='Invoices'?data.invoices:section==='Payroll'?data.payroll:section==='AI Estimates'?data.estimates:[];const action=section==='AI Estimates'?'Create AI Estimate':section==='Invoices'?'Create Invoice':'Add '+section.replace(/s$/,'');return <div className="workspace"><Head title={section} text="Organized inside this company workspace."/>{rows.length?rows.map((x:any)=><div className="wideRow" key={x.id}><div className="grow"><b>{x.project||x.customer||x.employee||section}</b><span className={"statusText status-"+String(x.status||'draft').toLowerCase().replace(/\s+/g,'-')}>{x.status||x.category||'Draft'}</span></div>{x.amount!==undefined&&<strong>{money(x.amount)}</strong>}</div>):<EmptyAction icon={section==='AI Estimates'?Sparkles:FileText} text={"No "+section.toLowerCase()+" yet."} action={action} onClick={()=>{if(section==='AI Estimates')location.href='/build-plans'}}/>}</div>}
function Head({title,text}:{title:string,text:string}){return <div className="workspaceHead"><div><h2>{title}</h2><p>{text}</p></div></div>}
function Stat({label,value,sub}:{label:string,value:string,sub:string}){return <div className="stat"><span>{label}</span><strong>{value}</strong><small>{sub}</small></div>}
function Field({label,value,set}:{label:string,value:string,set:(v:string)=>void}){return <label>{label}<input value={value} onChange={e=>set(e.target.value)}/></label>}
